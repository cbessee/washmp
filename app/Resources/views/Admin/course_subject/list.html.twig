{%  extends 'base.html.twig' %}

{% block stylesheets %}
    <link href="{{ asset('/global/plugins/datatables/datatables.min.css') }}" rel="stylesheet" type="text/css"/>
    <link href="{{ asset('/global/plugins/datatables/plugins/datatables.bootstrap.css') }}" rel="stylesheet" type="text/css"/>
    {{ parent() }}
{%  endblock %}

{%  block body %}
    <div class="page-bar">
        <ul class="page-breadcrumb">
            <li>
                <a href="{{ path('homepage') }}">Home</a>
                <i class="fa fa-circle"></i>
            </li>
            <li>
                <a href="{{ path('admin') }}">Admin</a>
                <i class="fa fa-circle"></i>
            </li>
            <li>
                <a href="{{ path('list_course_subjects') }}">Course Subjects</a>
            </li>
        </ul>
    </div>

    <h1 class="page-title">Course Subjects <small>View All Course Subjects</small></h1>

    <div class="row">
        <div class="col-md-12">
            <div class="portlet box red-mint">
                <div class="portlet-title">
                    <div class="caption">
                        <i class="fa fa-user"></i>
                        <span class="caption-subject bold uppercase">Course Subjects</span>
                    </div>
                    <div class="actions">
                        <a href="{{ path('new_course_subject') }}" class="btn btn-circle btn-default">
                            <i class="fa fa-plus"></i>
                            New Course Subject
                        </a>
                    </div>
                </div>
                <div class="portlet-body">
                    <table class="table table-striped table-bordered table-hover">
                        <thead>
                            <tr>
                                <th>Subject Area</th>
                                <th>Actions</th>
                            </tr>
                        </thead>
                        <tbody>
                            {% for course_subject in course_subjects %}
                            <tr>
                                <td>{{ course_subject.subjectArea }}</td>
                                <td>
                                    <a href="{{ path('edit_course_subject', {'id': course_subject.subjectID}) }}" class="btn btn-circle btn-default">
                                        <i class="fa fa-pencil"></i>
                                        Edit Course Subject
                                    </a>
                                    <a href="{{ path('delete_course_subject', {'id': course_subject.subjectID}) }}" class="btn btn-circle btn-default">
                                        <i class="fa fa-ban"></i>
                                        Delete Course Subject
                                    </a>
                                </td>
                            </tr>
                            {% endfor %}
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    </div>
{%  endblock %}

{%  block javascripts %}
    {{ parent() }}
    <script src="{{ asset('/global/plugins/datatables/datatables.min.js') }}" type="text/javascript"></script>
    <script src="{{ asset('/global/scripts/datatable.min.js') }}" type="text/javascript"></script>
    <script>
        $(document).ready(function () {
            $('.table').dataTable();
        })
    </script>
{%  endblock %}