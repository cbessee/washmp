{{ form_start(courseForm) }}
<div class="form-body row">
    <h3 class="col-md-12">District Info</h3>
    <div class="col-md-6">
        {{ form_row(courseForm.courseName, {
            'label': 'Course Name',
        }) }}
    </div>
    <div class="col-md-6">
        {{ form_row(courseForm.stateCourseCode, {
            'label': 'State Course Code',
        }) }}
    </div>
    <div class="col-md-6">
        {{ form_row(courseForm.subject, {
            'label': 'Subject Area',
        }) }}
    </div>
</div>
<div class="form-actions">
    <div class="row">
        <div class="col-md-offset-3 col-md-9">
            <button class="btn btn-circle green" type="submit" formnovalidate>Save</button>
        </div>
    </div>
</div>
{{ form_end(courseForm) }}